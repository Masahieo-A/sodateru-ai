# 構成メモ（開発者向け）

- **GitHubリポジトリ**: `Masahieo-A/sodateru-ai`（`git remote get-url origin` で確認: `git@github.com:Masahieo-A/sodateru-ai.git`）
- **フレームワーク**: Next.js 16.2.6（App Router） / React 19.2.4 / TypeScript ^5 / Tailwind CSS ^4（package.json の dependencies / devDependencies から確認）
- **DB / データ管理**: Supabase（`@supabase/supabase-js` ^2.106.2 を使用）。`supabase/` ディレクトリあり（`supabase/schema.sql` にDBスキーマ定義）
- **APIキー管理**: `.env.local`（gitignore対象）と `.env.example` が存在。含まれるキー名は以下（値は非記載）:
  - `GEMINI_API_KEY`
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `TEACHER_PASSWORD`
  - 本番はVercel環境変数（.vercel/project.json の存在からVercelデプロイ済みと確認）
- **デプロイ先**: Vercel（`.vercel/project.json` より projectName: `sodateru-ai` / projectId: `prj_O4TsGSy5WvrAUgu1MREdq7sUFAGC`）
- **関連ドキュメント**: 機能要件は `docs/要件定義.md` を参照
